/**
 * The audited release candidate: contracts/clause_protocol.py (LF line endings, ASCII).
 * tests/deploy.test.ts fails if this constant and the file ever diverge, so the deploy tool can only ever deploy the audited bytes.
 */
export const EXPECTED_CONTRACT_SHA256 = "07d7e4bfdbaeeba2d30129c00034cd728681ed99877286a16189005fbe7d847e";
export const EXPECTED_CONTRACT_BYTES = 134456;
export const EXPECTED_PATH = "contracts/clause_protocol.py";
