# Release security matrix (Stage 6A) - audited file SHA-256 `07d7e4bf…847e`

Method: (1) read the guard in the source (line numbers refer to the audited file); (2) **independent effectiveness check**: `scripts/security_mutation_sweep.py` disables each guard in turn and requires the protecting tests to fail (16 of 18 killed; the 2 survivors are provably redundant, see below; raw results `docs/RELEASE_MUTATION_SWEEP.json`); (3) list the residual risk that no local test can close. "Verified locally" never means "verified on StudioNet".

| Area | Mechanism (source) | Independent check this stage | Residual / not provable locally |
|---|---|---|---|
| Authorization | Program ops `_require_manufacturer` (L1216, used L1225-1495); `file_claim` holder-only (L1701); `respond_to_claim` manufacturer-only (L1776); evidence/challenge party checks (`_require_party` L2140); withdrawal recipient-only (L2617); finalize/settle/freeze/adjudicate/resolve permissionless but only over frozen state | Sweep: holder check, manufacturer check, recipient check each **killed** | Address encoding of the caller under real StudioNet (`_sender()`) unverified live |
| Frozen Constitution | `is_frozen` set inside `issue_warranty` (L1526-1527) in the same tx as the first passport; frozen-terms tests assert every write method leaves it untouched (`test_constitution_hardening`) | Exhaustive write-method inventory test (a new write method fails the suite until audited) | none known |
| Source eligibility | `https` only + exact-host / `*.base` rule (`_host_allowed` L129, L1848), decided at submission, before any fetch; ineligible records never retrieved or adjudicated | Sweep: host policy **killed**; 18 lookalike/suffix/prefix attack tests | Redirect behaviour of real `web.get` unverified (Gate A/E cover retrieval) |
| Evidence cap | ELIGIBLE records counted over stored state at submit (L1859), cap 10; adjudication asserts the invariant | Sweep **killed**; 9/10/11 boundary tests | first-come fairness (disclosed) |
| Evidence immutability | submission requires status DISPUTED (L1826) and not frozen; freeze processes each record once; corrections cannot cite non-frozen evidence | Sweep: removing the *explicit* frozen check **survived** because the DISPUTED-status guard (L1826) already forbids submission after freeze (status becomes EVIDENCE_FROZEN) - redundant defence in depth, behaviour identical | Retrieval consensus itself (Gate A/B) |
| Adjudication validation | `_check_model_result` (L279): exact keys, enums, ids ⊆ targeted / exclusions / shown evidence, coherence rules, rationale bound; re-applied by every validator; outcome derived by `_derive_outcome`, never model-supplied | Sweep: covered-clause membership **killed**; 37 malformed shapes + 21 validator-replay tests | Real model behaviour (Gate E/F) |
| Challenge validation | `_check_challenge_result` (L874): exact keys, per-ground correctable fields, merged result re-validated by the Stage 3 checker, cited-item coupling, materiality; deterministic gates first | Sweep: ground allow-list **killed**; 118 + 115 tests incl. 25 forbidden economic/constitution keys | Real model (Gate G) |
| Remedy completeness | `_check_remedy_completeness` (L577) called at constitution creation (L1316), before any passport can bind | Sweep **killed**; exhaustive 32-table oracle test; real-simulator refusal | none known |
| Reservation accounting | one reservation per warranty; settlement moves `min(remedy, remaining)` (pool total, reserved, reservation, claimable) in one step; release guarded by grace + all-claims-settled (L1604 area); conservation checked by an independent ledger + 6 seeded random adversarial runs | Sweep: settlement cap **killed**, grace **killed**, unsettled-claim guard **killed** | The 3-part invariant depends on exact u256 arithmetic under GenVM (verified locally only) |
| Settlement | `finalize_claim` (one `FinalDecision`, L2512) then `settle_claim` (one-shot `settled_at`, L2589-2590); remedy only from the frozen table via `_select_remedy` | Sweep: no-finalize-while-window-open **killed**; lifecycle A-G | n/a |
| Withdrawal | pull payment; `withdrawn_at` and `claimable=0` written before `emit_transfer` (L2622-2625); recipient-only | Sweep: recipient **killed**, double-withdraw **killed**; ledger reconciles emitted transfers | **Transfer emission timing/finality on StudioNet (Gate H)**; glsim tracks no balances |
| Double execution | one challenge / remand / finalize / settle / withdraw per claim; status machine forbids re-entry; a repeated call sees nothing to take | Sweep: the explicit one-adjudication check **survived**: redundant because `adjudicate_claim` requires status EVIDENCE_FROZEN (L1981) and the first adjudication moves it to DECIDED - identical behaviour | none known |
| Timestamp boundaries | all comparisons on protocol `_now()` (L446), inclusive `<=` at claim / response / challenge deadlines; `failure_asserted_at` never used for deadlines | Sweep: claim, response and challenge boundaries **killed**; +/-1 s boundary tests | **Protocol timestamp source on StudioNet (Gate C)** |
| Malformed model responses | fail closed, no repair; provider error strings rejected as non-dict; nothing written before the consensus block returns (atomic by structure) | 37 + 33 malformed cases assert `[LLM_ERROR]` and no state change | Undetermined-consensus rollback on the real network (Gate B) |
| Prompt-injection boundaries | evidence/explanations in JSON string slots, instructions before data, no browsing in adjudication/challenge, outputs cannot carry outcome/amount/recipient; no money/identity data in prompts (test-enforced) | injection tests for evidence and challenge text; hijacked-model tests (extra keys/outcome smuggling rejected) | **Real-model injection resistance (Gate F)** |
| Frontend authoritative rereads | `runTransaction` rereads contract state only after FINALIZED+SUCCESS; FINALIZED+ERROR/unknown = failure; finality gate fails closed; amounts exact bigint; single address source | 24 tx/lifecycle tests, 4 finality-gate UI tests, real-contract integration test | wallet signing / StudioNet response shape (Stage 6) |

## Other findings of this audit (no contract change)
- **Unbounded claims per warranty:** there is no cap on the number of claims a warranty can accumulate (each needs the previous to be past dispute, so it costs the holder transactions). `cancel_warranty`, `release_expired_reservation` and `get_resolution_receipt`/`list_*` iterate them linearly. Low risk (self-inflicted cost), noted for a future version.
- **`fund_pool` is open to anyone** (donations only increase the manufacturer's pool; deliberate and trivial by design, no revert path after value arrives).
- **Line-ending hazard fixed:** the working copy was CRLF while Git stores LF (the earlier on-disk hash `1e990046…` differed from the committed content's `07d7e4bf…`). The file is now LF on disk, identical to the blob at the Stage 4.5 freeze; `.gitattributes` (`*.py text eol=lf`) keeps it so, and the deployer/test enforce the LF hash.
- No new contract defect was found; the contract was not modified.

## Verdict on security
No blocking finding. Open items are exactly the StudioNet gates (A-H) plus the human cancellation-policy decision (`docs/CANCELLATION_POLICY_DECISION.md`).
